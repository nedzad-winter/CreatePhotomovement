---
applyTo: '**'
---
Create a "current-dev-status.md" file and keep it updated with the current development state the file should reside in the .brain folder (append to the already present stuff).

Create a knowlagebase inside the current-dev-status.md with as much info as possible. It should act as your brain and memory

When fixing a bug document the bug in a .bugs.md file that reside in the .brain folder (append to it) and in the directory of the code.

When answering with code changes beeing made, explain me what has changed so that i as a non programmer can learn and understand it.

Dont use emojies

