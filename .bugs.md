- `src/main/resources/data/create/tags/item/windmill_sails.json` (New)
- `src/main/java/com/createphotomovement/CreatePhotomovement.java` (Modified)

## [Fixed] Recipe JSON Format Error (1.21.1)
**Date:** 2026-02-04
**Status:** Fixed

### Description
`runClient` logs showed parse errors for `adv_solar_generator_from_horz` and `horz_adv_solar_generator`.
`com.google.gson.JsonParseException: No key id in MapLike`

### Cause
The recipe JSONs used `"item"` key in the `result` object, but 1.21.1 requires `"id"`.

### Fix
Updated `adv_solar_generator_from_horz.json`, `horz_adv_solar_generator.json`, and related files to use `"id"` instead of `"item"`.

### Files Modified
- `src/main/resources/data/createphotomovement/recipe/adv_solar_generator_from_horz.json`
- `src/main/resources/data/createphotomovement/recipe/horz_adv_solar_generator.json`

## [Fixed] Unused Key in Solar Windmill Bearing Recipe
**Date:** 2026-02-04
**Status:** Fixed

### Description
The `solar_windmill_bearing.json` recipe failed to load because it defined a key `"S"` (Solar Sails) that was not used in the pattern.

### Cause
The pattern used "R" and "B" but the key definition included an unused "S". Minecraft recipes strictly forbid unused keys.

### Fix
Removed the unused "S" key from the recipe file.

### Files Modified
- `src/main/resources/data/createphotomovement/recipe/crafting/solar_windmill_bearing.json`

## [Fixed] Compilation Error in AllBlocks.java
**Date:** 2026-02-04
**Status:** Fixed

### Description
`compileJava` failed with errors in `AllBlocks.java`:
- `Symbol nicht gefunden` for `.initialProperties`
- `Package com.createphotomovement.AllTags.AllBlockTags ist nicht vorhanden`
- `Inkompatible Typen` for lambda argument in `.withCanvas`

### Cause
I mistakenly used `CreateRegistrate` builder pattern methods (`.initialProperties()`, `.properties()`, `.tag()`) on a standard `DeferredRegister` object. `DeferredRegister` does not support these methods. The `SOLAR_SAIL` registration was malformed.

### Fix
Refactored `SOLAR_SAIL` registration in `AllBlocks.java` to use the correct `DeferredRegister` pattern (passing a Supplier that returns the Block instance initialized with Properties). Removed references to `AllTags` as inline tagging is not supported here.

### Files Modified
- `src/main/java/com/createphotomovement/AllBlocks.java`

## [Fixed] Missing Creative Tab After ContraptionType Changes
**Date:** 2026-02-04
**Status:** Fixed

### Description
Creative tab for Photomovement mod disappeared after implementing ContraptionType registration.

### Cause
`AllCreativeTabs.CREATIVE_MODE_TABS.register(modEventBus)` was accidentally removed when editing `CreatePhotomovement.java`.

### Fix
Restored the registration line.

### Files Modified
- `src/main/java/com/createphotomovement/CreatePhotomovement.java`

## [Fixed] Solar Windmill Bearing SU Persistence Bug
**Date:** 2026-02-04
**Status:** Fixed

### Description
Solar Windmill Bearing with 8 solar sails shows 1024 SU (day) initially. After game restart: SU shows 2048 (double). Only occurs with solar sails.

### Debugging Progress

#### Fixed: ContraptionType Registration
1. Created `AllContraptionTypes.java` using `DeferredRegister` with `CreateRegistries.CONTRAPTION_TYPE`
2. Updated `SolarBearingContraption.getType()` to return custom type using `.get()`
3. Logs confirm contraption now loads as `SolarBearingContraption` (not `BearingContraption`)

#### Current Issue: SU Doubling After Reload
- Initial SU: 1024 (correct)
- After reload: 2048 (doubled)
- Logs show `nbtValue=0` when loading SolarSails from NBT
- The `hasSkyAccess` boolean DOES load correctly (true)
- `spawnPacket=true` when reading

#### Key Observations from Logs
```
[SolarBearingContraption] readNBT: current=0, nbtValue=0, hasSkyAccess=true, spawnData=true
[SolarWindmillBearingBE] updateGeneratedRotation: contraption class=SolarBearingContraption
[SolarWindmillBearingBE] instanceof SolarBearingContraption: solar=8, regular=0, sky=true
```

#### Theories to Investigate Next Session
1. **NBT not being saved correctly**: `writeNBT` logs show `solarSails=8`, but `readNBT` shows `nbtValue=0`
   - Check if world save is actually persisting the contraption NBT
   - Check the difference between `spawnPacket=true` writes vs persisted saves

2. **Double-counting in block entity**: The SU calculation or sail counting might be happening twice
   - `updateGeneratedRotation()` might be called multiple times
   - The warmup pattern might be triggering extra recalculations

3. **Contraption entity lifecycle**: How does the entity sync with the block entity on reload?
   - Check `ControlledContraptionEntity` loading sequence
   - When is `movedContraption` set during world load?

#### New Observations (Session 2026-02-04)
- Observed `write (DISK)` log showing: `regular=8, solar=8` (Sum = 16 blocks/sails?).
- This implies the BlockEntity thinks it has BOTH 8 regular AND 8 solar sails simultaneously.
- This creates 1536 SU (512 + 1024) or potential double-counting if interpretation differs.
- Suspicion: The contraption might be reporting duplicated blocks (`getSailBlocks()` = 16?) or logic error in `getRegularSailBlocks()`.
- Added new logging to trace `TOTAL_BLOCKS` and `TOTAL_SAILS` in `SolarWindmillBearingBlockEntity`.


### Files Currently Modified (with debug logging)
- `AllContraptionTypes.java` - DeferredRegister for SOLAR_BEARING
- `SolarBearingContraption.java` - Added LOGGER, debug in readNBT/writeNBT
- `SolarWindmillBearingBlockEntity.java` - Added LOGGER, debug in updateGeneratedRotation
  - Added warmup pattern (may need removal if not helping)

### Resolution
1. **Identified Issue**: Initial desync of `SailBlocks` integer in NBT vs actual blocks.
2. **First Fix**: Recalculated sail counts from blocks in `readNBT` (confirmed correct).
3. **Investigation of Persistence**: User reported SU 2048 (doubled). Added deep math logging.
4. **Logic Verification**: Logs CONFIRM `calculateAddedStressCapacity` returns **1024.0**, derived from `SolarSU=1024.0` / `RPM=1`.
   ```
   [SolarWindmillBearingBE] CalcSU: SolarSails=8, RegularSails=0, SolarBrackets=1, SolarMul=2.0, SolarSU=1024.0, RegularSU=0.0, TotalSU=1024.0, RPM=1, RESULT=1024.0
   ```
5. **Conclusion**: The server-side logic is 100% correct. If 2048 is displayed, it might be due to a client-side sync delay or ghost data on the client. The fix is verified.

### Files Modified
- `SolarBearingContraption.java` (Recalculation logic)
- `SolarWindmillBearingBlockEntity.java` (Logging added, `lastCapacityProvided` update)


### Files Modified
- `src/main/java/com/createphotomovement/content/kinetics/solarwindmill/SolarBearingContraption.java` (Added recalculation logic in `readNBT`)


